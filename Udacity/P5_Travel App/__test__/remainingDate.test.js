// Import the js file to test
import { remainingDate } from "../src/client/js/remainingDate"

describe("Testing the submit functionality", () => {
    test("Testing the remainingDate() function", () => {
    expect(remainingDate).toBeDefined();
})});