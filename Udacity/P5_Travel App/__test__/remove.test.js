// Import the js file to test
import { remove } from "../src/client/js/remove"

describe("Testing the submit functionality", () => {
    test("Testing the remove() function", () => {
    expect(remove).toBeDefined();
})});